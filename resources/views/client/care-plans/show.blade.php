<x-layouts.app :title="$plan->name.' — Care & Support'"><div class="mb-7"><p class="text-sm font-medium text-indigo-600">Care & Support</p><h1 class="text-3xl font-semibold">{{ $plan->name }}</h1><p class="mt-1 text-slate-500">{{ str($plan->status)->title() }} · Next billing {{ $plan->next_billing_date?->format('M j, Y')??'to be confirmed' }}</p></div><div class="grid gap-6 lg:grid-cols-2"><section class="rounded-2xl border border-slate-200 bg-white p-6"><h2 class="font-semibold">Included services</h2><ul class="mt-4 list-disc space-y-2 pl-5 text-sm text-slate-600">@foreach($plan->included_services??[] as $service)<li>{{ $service }}</li>@endforeach</ul><div class="mt-6 rounded-xl bg-slate-50 p-4"><div class="flex justify-between text-sm"><span>Included support</span><span>{{ $plan->used_support_minutes }} / {{ $plan->included_support_minutes }} minutes used</span></div><p class="mt-2 text-xs text-slate-500">Additional work: {{ $plan->currency }} {{ number_format($plan->additional_hourly_rate,2) }}/hour</p></div></section><section class="rounded-2xl border border-slate-200 bg-white p-6"><h2 class="font-semibold">Service status</h2><dl class="mt-4 space-y-4 text-sm"><div class="flex justify-between"><dt class="text-slate-500">Service</dt><dd>{{ str($plan->service_status)->replace('_',' ')->title() }}</dd></div><div class="flex justify-between"><dt class="text-slate-500">SSL</dt><dd>{{ str($plan->ssl_status)->replace('_',' ')->title() }}</dd></div><div class="flex justify-between"><dt class="text-slate-500">Last backup</dt><dd>{{ $plan->last_backup_at?->format('M j, Y')??'Not recorded' }}</dd></div><div class="flex justify-between"><dt class="text-slate-500">Last maintenance</dt><dd>{{ $plan->last_maintenance_at?->format('M j, Y')??'Not recorded' }}</dd></div></dl></section></div><section class="mt-6 rounded-2xl border border-slate-200 bg-white p-6"><h2 class="font-semibold">Recent maintenance</h2><div class="mt-4 space-y-3">@forelse($plan->activities->take(10) as $activity)<div class="flex justify-between border-b border-slate-100 pb-3 text-sm"><div><p class="font-medium">{{ str($activity->type)->title() }}</p><p class="text-slate-500">{{ $activity->notes }}</p></div><span>{{ $activity->occurred_at->format('M j, Y') }}</span></div>@empty<p class="text-sm text-slate-500">No maintenance activity recorded yet.</p>@endforelse</div></section></x-layouts.app>
