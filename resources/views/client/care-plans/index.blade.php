<x-layouts.app title="Care & Support — Ikira Client Portal"><div class="mb-7"><h1 class="text-3xl font-semibold">Care & Support</h1><p class="mt-1 text-slate-500">Your maintenance, hosting and support coverage.</p></div><div class="grid gap-4">@forelse($plans as $plan)<a href="{{ route('client.care-plans.show',$plan) }}" class="rounded-2xl border border-slate-200 bg-white p-5"><div class="flex justify-between"><div><p class="font-semibold">{{ $plan->name }}</p><p class="text-sm text-slate-500">{{ $plan->project?->name ?? str($plan->type)->replace('_',' ')->title() }}</p></div><div class="text-right"><p>{{ $plan->currency }} {{ number_format($plan->monthly_price,2) }}/mo</p><p class="text-xs text-slate-500">{{ str($plan->status)->title() }}</p></div></div></a>@empty<p class="rounded-2xl border border-slate-200 bg-white p-8 text-center text-slate-500">You do not have an active Care & Support plan.</p>@endforelse</div></x-layouts.app>
