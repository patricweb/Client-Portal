<x-layouts.app title="Requests — Ikira Client Portal"><div class="mb-7 flex items-center justify-between gap-4"><div><h1 class="text-3xl font-semibold">Requests</h1><p class="mt-1 text-slate-500">Ask for support, updates or additional work.</p></div><a href="{{ route('client.requests.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white">New request</a></div><div class="space-y-4">@forelse($requests as $item)<a href="{{ route('client.requests.show',$item) }}" class="flex justify-between rounded-2xl border border-slate-200 bg-white p-5"><div><p class="font-semibold">#{{ $item->id }} {{ $item->subject }}</p><p class="text-sm text-slate-500">{{ $item->project?->name }} · {{ str($item->category)->replace('_',' ')->title() }}</p></div><span class="text-sm">{{ str($item->status)->replace('_',' ')->title() }}</span></a>@empty<p class="rounded-2xl border border-slate-200 bg-white p-8 text-center text-slate-500">No requests yet.</p>@endforelse</div></x-layouts.app>
