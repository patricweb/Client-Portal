<x-layouts.app :title="$document->title.' — Ikira Client Portal'">
    <div class="mb-7 flex flex-wrap items-start justify-between gap-4"><div><p class="text-sm font-medium text-indigo-600">{{ str($document->type)->replace('_',' ')->title() }} · Version {{ $document->current_version }}</p><h1 class="mt-1 text-3xl font-semibold">{{ $document->title }}</h1><p class="mt-1 text-slate-500">Status: {{ str($document->status)->replace('_',' ')->title() }}</p></div><a href="{{ route('client.documents.pdf', $document) }}" class="rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-medium">Download PDF</a></div>
    <section class="rounded-2xl border border-slate-200 bg-white p-6"><div class="prose max-w-none">{!! $currentVersion?->content !!}</div></section>
    @if($document->status==='awaiting_approval')<section class="mt-6 rounded-2xl border border-indigo-200 bg-indigo-50 p-5"><h2 class="font-semibold">Approval required</h2><form method="POST" action="{{ route('client.documents.decision', $document) }}" class="mt-4 space-y-3">@csrf<textarea name="comment" rows="3" class="w-full rounded-lg border border-slate-300 p-3" placeholder="Comment (required when requesting changes)"></textarea><div class="flex gap-3"><button name="decision" value="approved" class="rounded-lg bg-emerald-600 px-4 py-2 text-sm font-medium text-white">Approve</button><button name="decision" value="changes_requested" class="rounded-lg bg-white px-4 py-2 text-sm font-medium text-slate-800 ring-1 ring-slate-300">Request changes</button></div></form></section>@endif
</x-layouts.app>
