<x-layouts.app :title="$project->name.' — Ikira Client Portal'">
    <div class="mb-7 flex flex-wrap items-start justify-between gap-4"><div><p class="text-sm font-medium text-indigo-600">{{ str($project->type)->replace('_',' ')->title() }}</p><h1 class="mt-1 text-3xl font-semibold">{{ $project->name }}</h1><p class="mt-1 text-slate-500">{{ $project->description }}</p></div>@if($project->brief)<a href="{{ route('client.brief.edit', $project) }}" class="rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-medium">Brief: {{ str($project->brief->status)->replace('_',' ')->title() }}</a>@endif</div>
    @if($project->brief && in_array($project->brief->status, ['submitted', 'approved']))@include('partials.brief-answers', ['brief' => $project->brief, 'class' => 'mb-6'])@endif
    <div class="grid gap-6 xl:grid-cols-[1fr_340px]"><section class="rounded-2xl border border-slate-200 bg-white p-5"><h2 class="font-semibold">Project timeline</h2><div class="mt-6">@foreach($project->stages as $stage)<div class="relative grid grid-cols-[28px_1fr] gap-3 pb-6 last:pb-0"><div class="relative"><span class="relative z-10 grid size-7 place-items-center rounded-full {{ in_array($stage->status, ['approved','completed']) ? 'bg-emerald-500 text-white' : ($stage->status === 'in_progress' ? 'bg-indigo-500 text-white' : 'bg-slate-100 text-slate-500') }} text-xs">{{ in_array($stage->status, ['approved','completed']) ? '✓' : $stage->position }}</span>@if(!$loop->last)<span class="absolute left-3.5 top-7 h-full w-px bg-slate-200"></span>@endif</div><div><div class="flex flex-wrap items-center gap-2"><h3 class="font-medium">{{ $stage->title }}</h3><span class="rounded-full bg-slate-100 px-2 py-0.5 text-xs text-slate-600">{{ str($stage->status)->replace('_',' ')->title() }}</span></div><p class="mt-1 text-sm text-slate-500">{{ $stage->client_description }}</p>@if($stage->due_date)<p class="mt-1 text-xs text-slate-400">Due {{ $stage->due_date->format('M j, Y') }}</p>@endif @if($stage->requires_approval && $stage->status==='approval_required')<form method="POST" action="{{ route('client.stages.decision', [$project,$stage]) }}" class="mt-3 flex flex-wrap gap-2">@csrf<input name="comment" class="min-w-60 flex-1 rounded-lg border border-slate-300 px-3 py-2 text-sm" placeholder="Comment for requested changes"><button name="decision" value="approved" class="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-medium text-white">Approve</button><button name="decision" value="changes_requested" class="rounded-lg border border-slate-300 bg-white px-3 py-2 text-xs font-medium">Request changes</button></form>@endif</div></div>@endforeach</div></section>
    <div class="space-y-6"><section class="rounded-2xl border border-slate-200 bg-white p-5"><h2 class="font-semibold">Progress</h2><p class="mt-3 text-3xl font-semibold">{{ $project->progress }}%</p><div class="mt-3 h-2 rounded-full bg-slate-100"><div class="h-2 rounded-full bg-indigo-500" style="width:{{ $project->progress }}%"></div></div><p class="mt-3 text-sm text-slate-500">Target: {{ $project->target_completion_date?->format('M j, Y') ?? 'To be confirmed' }}</p>@if($project->staging_url)<a target="_blank" href="{{ $project->staging_url }}" class="mt-4 inline-block text-sm font-medium text-indigo-600">Open preview →</a>@endif</section>
    <section class="rounded-2xl border border-slate-200 bg-white p-5"><h2 class="font-semibold">Files</h2><div class="mt-4 space-y-2">@forelse($project->attachments as $attachment)<a href="{{ route('attachments.download', $attachment) }}" class="block truncate text-sm text-indigo-600">{{ $attachment->original_name }}</a>@empty<p class="text-sm text-slate-500">No files yet.</p>@endforelse</div><form method="POST" enctype="multipart/form-data" action="{{ route('client.projects.attachments.store', $project) }}" class="mt-4">@csrf<input type="file" name="file" required class="block w-full text-sm"><button class="mt-3 rounded-lg bg-slate-900 px-3 py-2 text-sm text-white">Upload file</button></form></section></div></div>
</x-layouts.app>
