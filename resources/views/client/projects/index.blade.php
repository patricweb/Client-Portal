<x-layouts.app title="Projects — Ikira Client Portal"><div class="mb-7"><h1 class="text-3xl font-semibold">Projects</h1><p class="mt-1 text-slate-500">Everything Ikira is building for your company.</p></div><div class="grid gap-4 md:grid-cols-2">@forelse($projects as $project)<a href="{{ route('client.projects.show', $project) }}" class="rounded-2xl border border-slate-200 bg-white p-5 hover:border-indigo-300"><div class="flex justify-between"><div><h2 class="font-semibold">{{ $project->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ str($project->type)->replace('_',' ')->title() }}</p></div><span class="text-sm">{{ $project->progress }}%</span></div><div class="mt-5 h-2 rounded-full bg-slate-100"><div class="h-2 rounded-full bg-indigo-500" style="width:{{ $project->progress }}%"></div></div><p class="mt-4 text-sm text-slate-500">{{ str($project->status)->replace('_',' ')->title() }} · Target {{ $project->target_completion_date?->format('M j, Y') ?? 'TBC' }}</p></a>@empty<p class="text-slate-500">No projects have been added yet.</p>@endforelse</div></x-layouts.app>

