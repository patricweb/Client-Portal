<x-layouts.app title="Billing — Ikira Client Portal"><div class="mb-7"><h1 class="text-3xl font-semibold">Billing</h1><p class="mt-1 text-slate-500">Invoices, payments and remaining balance.</p></div><div class="mb-6 grid gap-4 sm:grid-cols-3"><div class="rounded-2xl border border-slate-200 bg-white p-5"><p class="text-sm text-slate-500">Total invoiced</p><p class="mt-2 text-2xl font-semibold">{{ number_format($total,2) }}</p></div><div class="rounded-2xl border border-slate-200 bg-white p-5"><p class="text-sm text-slate-500">Paid</p><p class="mt-2 text-2xl font-semibold text-emerald-700">{{ number_format($paid,2) }}</p></div><div class="rounded-2xl border border-slate-200 bg-white p-5"><p class="text-sm text-slate-500">Remaining</p><p class="mt-2 text-2xl font-semibold">{{ number_format(max(0,$total-$paid),2) }}</p></div></div><div class="space-y-4">@forelse($invoices as $invoice)<a href="{{ route('client.billing.show',$invoice) }}" class="flex items-center justify-between rounded-2xl border border-slate-200 bg-white p-5"><div><p class="font-semibold">{{ $invoice->invoice_number }}</p><p class="text-sm text-slate-500">{{ $invoice->project?->name }} · Due {{ $invoice->due_date->format('M j, Y') }}</p></div><div class="text-right"><p class="font-medium">{{ $invoice->currency }} {{ number_format($invoice->total,2) }}</p><p class="text-xs text-slate-500">{{ str($invoice->status)->replace('_',' ')->title() }}</p></div></a>@empty<p class="rounded-2xl border border-slate-200 bg-white p-8 text-center text-slate-500">No invoices yet.</p>@endforelse</div></x-layouts.app>
