<x-layouts.app :title="'Project brief — '.$project->name"><div class="mx-auto max-w-3xl"><div class="mb-7"><p class="text-sm font-medium text-indigo-600">{{ $project->name }}</p><h1 class="mt-1 text-3xl font-semibold">Project brief</h1><p class="mt-1 text-slate-500">Save a draft at any time. Submit when your answers are complete.</p></div>
@if(in_array($brief->status, ['submitted','approved']))<div class="rounded-2xl border border-emerald-200 bg-emerald-50 p-5 text-emerald-800">This brief was {{ $brief->status }}{{ $brief->submitted_at ? ' on '.$brief->submitted_at->format('M j, Y') : '' }}. Your submitted answers are shown below. Contact Ikira if you need to change them.</div>@include('partials.brief-answers', ['brief' => $brief, 'class' => 'mt-5'])@else
@php($answerMap = $brief->answers->pluck('value', 'brief_template_field_id'))
<form method="POST" action="{{ route('client.brief.update', $project) }}" class="space-y-5">@csrf @method('PUT')
    @forelse($brief->template?->fields ?? [] as $field)<label class="block rounded-2xl border border-slate-200 bg-white p-5 text-sm font-medium">{{ $field->label }} @if($field->is_required)<span class="text-red-500">*</span>@endif @if($field->help_text)<span class="mt-1 block font-normal text-slate-500">{{ $field->help_text }}</span>@endif<textarea name="answers[{{ $field->id }}]" rows="5" class="mt-3 w-full rounded-xl border border-slate-300 px-3 py-2.5 font-normal">{{ old("answers.{$field->id}", $answerMap->get($field->id)) }}</textarea>@error("answers.{$field->id}")<span class="mt-1 block text-red-600">{{ $message }}</span>@enderror</label>@empty<div class="rounded-2xl border border-amber-200 bg-amber-50 p-5 text-amber-800">Ikira is preparing the questions for this project.</div>@endforelse
    <div class="flex flex-wrap gap-3"><button class="rounded-xl border border-slate-300 bg-white px-5 py-3 font-medium">Save draft</button><button name="submit" value="1" class="rounded-xl bg-indigo-600 px-5 py-3 font-medium text-white">Submit brief</button></div>
</form>@endif</div></x-layouts.app>
