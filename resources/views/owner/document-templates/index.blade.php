<x-layouts.app title="Document templates — Ikira Client Portal">
    <div class="mb-7"><h1 class="text-3xl font-semibold">Document templates</h1><p class="mt-1 text-slate-500">Reusable legal and commercial content. Have legal templates reviewed before production use.</p></div>
    <div class="grid gap-6 xl:grid-cols-2"><form method="POST" action="{{ route('owner.document-templates.store') }}" class="space-y-4 rounded-2xl border border-slate-200 bg-white p-5">@csrf<h2 class="font-semibold">New template</h2><input name="name" required placeholder="Template name" class="w-full rounded-lg border border-slate-300 p-2.5"><input name="type" required placeholder="proposal" class="w-full rounded-lg border border-slate-300 p-2.5"><textarea name="content" rows="12" required class="w-full rounded-lg border border-slate-300 p-3 font-mono text-sm"></textarea><label class="flex gap-2 text-sm"><input type="checkbox" name="is_active" value="1" checked> Active</label><button class="rounded-lg bg-indigo-600 px-4 py-2 text-sm text-white">Create</button></form>
    <div class="space-y-5">@foreach($templates as $template)<form method="POST" action="{{ route('owner.document-templates.update', $template) }}" class="space-y-3 rounded-2xl border border-slate-200 bg-white p-5">@csrf @method('PUT')<input name="name" value="{{ $template->name }}" required class="w-full rounded-lg border border-slate-300 p-2.5"><input name="type" value="{{ $template->type }}" required class="w-full rounded-lg border border-slate-300 p-2.5"><textarea name="content" rows="8" required class="w-full rounded-lg border border-slate-300 p-3 font-mono text-sm">{{ $template->content }}</textarea><label class="flex gap-2 text-sm"><input type="checkbox" name="is_active" value="1" @checked($template->is_active)> Active</label><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm text-white">Update</button></form>@endforeach</div></div>
</x-layouts.app>
