<x-layouts.app title="Invoices — Ikira Client Portal">
    <div class="mb-7 flex items-center justify-between gap-4"><div><h1 class="text-3xl font-semibold">Invoices</h1><p class="mt-1 text-slate-500">Draft, sent, paid and overdue invoices.</p></div><a href="{{ route('owner.invoices.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white">New invoice</a></div>
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="p-4">Number</th><th class="p-4">Client</th><th class="p-4">Due</th><th class="p-4">Total</th><th class="p-4">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($invoices as $invoice)<tr><td class="p-4"><a href="{{ route('owner.invoices.show',$invoice) }}" class="font-medium text-indigo-600">{{ $invoice->invoice_number }}</a></td><td class="p-4">{{ $invoice->company->name }}</td><td class="p-4">{{ $invoice->due_date->format('M j, Y') }}</td><td class="p-4">{{ $invoice->currency }} {{ number_format($invoice->total,2) }}</td><td class="p-4">{{ str($invoice->status)->replace('_',' ')->title() }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">No invoices yet.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $invoices->links() }}</div>
</x-layouts.app>
