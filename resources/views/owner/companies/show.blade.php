<x-layouts.app :title="$company->name.' — Ikira Client Portal'">
    <div class="mb-7 flex items-start justify-between gap-4"><div><p class="text-sm font-medium text-indigo-600">Client company</p><h1 class="mt-1 text-3xl font-semibold">{{ $company->name }}</h1><p class="mt-1 text-slate-500">{{ $company->timezone }} · {{ $company->currency }}</p></div><div class="flex gap-2"><a href="{{ route('owner.companies.edit', $company) }}" class="rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-medium">Edit legal details</a><a href="{{ route('owner.projects.create', ['company' => $company->id]) }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white">Create project</a></div></div>
    @if(session('temporary_credentials'))<section class="mb-6 rounded-2xl border border-amber-300 bg-amber-50 p-5"><h2 class="font-semibold text-amber-900">Copy these credentials now</h2><p class="mt-1 text-sm text-amber-800">The password is shown only once. The client must replace it after first login.</p><div class="mt-4 grid gap-2 rounded-xl bg-white p-4 font-mono text-sm"><span>Email: {{ session('temporary_credentials.email') }}</span><span>Password: {{ session('temporary_credentials.password') }}</span></div></section>@endif
    <div class="grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border border-slate-200 bg-white p-5"><h2 class="font-semibold">Legal identity & contacts</h2><div class="mt-4 space-y-4"><div><p class="font-medium">{{ $company->billing_name ?: 'Legal name missing' }}</p><p class="text-sm text-slate-500">{{ $company->jurisdiction ?: 'Jurisdiction missing' }}</p><p class="mt-1 whitespace-pre-line text-xs text-slate-400">{{ $company->billing_address ?: 'Business address missing' }}</p></div>@foreach($company->contacts as $contact)<div class="border-t border-slate-100 pt-3"><p class="font-medium">{{ $contact->name }} @if($contact->is_primary)<span class="text-xs text-indigo-600">Primary</span>@endif</p><p class="text-sm text-slate-500">{{ $contact->job_title ?: 'Title missing' }} · {{ $contact->email }}</p><p class="text-xs text-slate-400">Portal: {{ $contact->user?->status?->value ?? 'not created' }}</p></div>@endforeach</div></section>
    <section class="rounded-2xl border border-slate-200 bg-white p-5 lg:col-span-2"><h2 class="font-semibold">Projects</h2><div class="mt-4 divide-y divide-slate-100">@forelse($company->projects as $project)<a href="{{ route('owner.projects.show', $project) }}" class="flex justify-between gap-4 py-3"><div><p class="font-medium">{{ $project->name }}</p><p class="text-sm text-slate-500">{{ str($project->type)->replace('_',' ')->title() }} · {{ str($project->status)->replace('_',' ')->title() }}</p></div><span>{{ $project->progress }}%</span></a>@empty<p class="py-5 text-sm text-slate-500">No projects yet.</p>@endforelse</div></section></div>
</x-layouts.app>
