<x-layouts.app title="Leads — Ikira Client Portal">
    <div class="mb-7 flex items-center justify-between"><div><h1 class="text-3xl font-semibold">Leads</h1><p class="mt-1 text-slate-500">Potential clients and next conversations.</p></div><a href="{{ route('owner.leads.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white">Add lead</a></div>
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="px-5 py-3">Company</th><th class="px-5 py-3">Contact</th><th class="px-5 py-3">Service</th><th class="px-5 py-3">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($leads as $lead)<tr><td class="px-5 py-4 font-medium">{{ $lead->company_name }}</td><td class="px-5 py-4"><div>{{ $lead->contact_name }}</div><div class="text-slate-500">{{ $lead->email }}</div></td><td class="px-5 py-4">{{ $lead->service ?: '—' }}</td><td class="px-5 py-4">{{ str($lead->status)->replace('_',' ')->title() }}</td><td class="px-5 py-4 text-right"><a class="text-indigo-600" href="{{ route('owner.leads.edit', $lead) }}">Edit</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No leads yet.</td></tr>@endforelse</tbody></table></div></div>
    <div class="mt-5">{{ $leads->links() }}</div>
</x-layouts.app>

