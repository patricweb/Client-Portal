<x-layouts.app title="Requests — Ikira Client Portal"><div class="mb-7"><h1 class="text-3xl font-semibold">Requests</h1><p class="mt-1 text-slate-500">Client work requiring operational attention.</p></div><form class="mb-5"><select name="status" onchange="this.form.submit()" class="rounded-lg border border-slate-300 bg-white p-2.5 text-sm"><option value="">All statuses</option>@foreach(['new','in_progress','waiting_for_client','estimate_sent','completed','closed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select></form><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="p-4">Request</th><th class="p-4">Client</th><th class="p-4">Priority</th><th class="p-4">Status</th><th class="p-4">Billing</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($requests as $item)<tr><td class="p-4"><a href="{{ route('owner.requests.show',$item) }}" class="font-medium text-indigo-600">#{{ $item->id }} {{ $item->subject }}</a><p class="text-xs text-slate-500">{{ str($item->category)->replace('_',' ')->title() }}</p></td><td class="p-4">{{ $item->company->name }}</td><td class="p-4">{{ str($item->internal_priority)->title() }}</td><td class="p-4">{{ str($item->status)->replace('_',' ')->title() }}</td><td class="p-4">{{ $item->billing_classification ? str($item->billing_classification)->replace('_',' ')->title() : 'Unclassified' }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">No requests.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $requests->links() }}</div></x-layouts.app>
