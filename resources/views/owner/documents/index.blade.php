<x-layouts.app title="Documents — Ikira Client Portal">
    <div class="mb-7 flex flex-wrap items-center justify-between gap-3"><div><h1 class="text-3xl font-semibold">Documents</h1><p class="mt-1 text-slate-500">Proposals, contracts, approvals and version history.</p></div><div class="flex gap-2"><a href="{{ route('owner.document-templates.index') }}" class="rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-medium">Templates</a><a href="{{ route('owner.documents.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white">New document</a></div></div>
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="p-4">Document</th><th class="p-4">Client</th><th class="p-4">Project</th><th class="p-4">Status</th><th class="p-4">Version</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($documents as $document)<tr><td class="p-4"><a class="font-medium text-indigo-600" href="{{ route('owner.documents.show', $document) }}">{{ $document->title }}</a><p class="text-xs text-slate-500">{{ str($document->type)->replace('_',' ')->title() }}</p></td><td class="p-4">{{ $document->company->name }}</td><td class="p-4">{{ $document->project?->name ?? '—' }}</td><td class="p-4">{{ str($document->status)->replace('_',' ')->title() }}</td><td class="p-4">v{{ $document->current_version }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">No documents yet.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $documents->links() }}</div>
</x-layouts.app>
