<x-layouts.app title="Care & Support — Ikira Client Portal"><div class="mb-7 flex items-center justify-between gap-4"><div><h1 class="text-3xl font-semibold">Care & Support</h1><p class="mt-1 text-slate-500">Maintenance plans, support usage and recurring billing.</p></div><a href="{{ route('owner.care-plans.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white">New plan</a></div><div class="grid gap-4">@forelse($plans as $plan)<a href="{{ route('owner.care-plans.show',$plan) }}" class="rounded-2xl border border-slate-200 bg-white p-5"><div class="flex justify-between gap-4"><div><p class="font-semibold">{{ $plan->name }}</p><p class="mt-1 text-sm text-slate-500">{{ $plan->company->name }}{{ $plan->project?' · '.$plan->project->name:'' }}</p></div><div class="text-right"><p class="font-medium">{{ $plan->currency }} {{ number_format($plan->monthly_price,2) }}/mo</p><p class="text-xs text-slate-500">{{ str($plan->status)->title() }}</p></div></div></a>@empty<p class="rounded-2xl border border-slate-200 bg-white p-8 text-center text-slate-500">No Care & Support plans yet.</p>@endforelse</div><div class="mt-4">{{ $plans->links() }}</div></x-layouts.app>
