<x-layouts.app title="Notifications — Ikira Client Portal"><div class="mb-7 flex items-center justify-between gap-4"><div><h1 class="text-3xl font-semibold">Notifications</h1><p class="mt-1 text-slate-500">Updates and actions that need your attention.</p></div><form method="POST" action="{{ route('notifications.read-all') }}">@csrf<button class="rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-medium">Mark all read</button></form></div><div class="space-y-3">@forelse($notifications as $notification)<a href="{{ route('notifications.read',$notification->id) }}" class="block rounded-2xl border {{ $notification->read_at?'border-slate-200 bg-white':'border-indigo-200 bg-indigo-50' }} p-5"><div class="flex justify-between gap-4"><div><p class="font-semibold">{{ $notification->data['title'] }}</p><p class="mt-1 text-sm text-slate-600">{{ $notification->data['message'] }}</p></div><span class="text-xs text-slate-500">{{ $notification->created_at->diffForHumans() }}</span></div></a>@empty<p class="rounded-2xl border border-slate-200 bg-white p-8 text-center text-slate-500">No notifications.</p>@endforelse</div><div class="mt-4">{{ $notifications->links() }}</div></x-layouts.app>
